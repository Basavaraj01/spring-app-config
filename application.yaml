eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: ${EUREKA_SERVER_ADDRESS:http://localhost:8761/eureka}
      
okta:
  oauth2:
    issuer: https://trial-4927233.okta.com/oauth2/default
    audience: api://default
spring:
  zipkin:
    baseUrl: http://host.docker.internal:9411/api/v2/spans
